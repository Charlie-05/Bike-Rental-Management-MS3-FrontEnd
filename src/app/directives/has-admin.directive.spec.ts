import { HasAdminDirective } from './has-admin.directive';

describe('HasAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAdminDirective();
    expect(directive).toBeTruthy();
  });
});
